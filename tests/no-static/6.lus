

function f (x : bool) returns (a : bool; b : int when a)
let
  a = x;
  b = 2 when a;
tel


function g (a : bool; b : int when a) returns (x : bool)
let
  x = a;
tel


function h () returns (b : bool)
let
  b = g(f(true));
tel

